<%= erb(:sports, layout: false, locals: { active: @sport_list.sport.id }) %>
<div class="large-4 columns left">
  <ul class="side-nav">
    <% @sport_list.events.each do |event| %>
      <li class="<%= event.id == active ? 'active' : '' %>"><a href="/sports/<%= @sport_list.sport.id %>/events/<%= event.id %>"><%= event.title %></a></li>
    <% end %>
  </ul>
</div>
