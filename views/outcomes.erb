<%= erb(:sport, layout: false, locals: { active: @sport_list.event.id }) %>
<div class="large-4 columns left">
  <ul class="side-nav">
    <% @sport_list.outcomes.each do |outcome| %>
      <li>Market: <%= outcome.market %>, <%= outcome.description %>: <%= outcome.price %></li>
    <% end %>
  </ul>
</div>
